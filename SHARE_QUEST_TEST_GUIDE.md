# 🔗 기존 공유 기능을 이용한 공유 퀘스트 테스트 가이드

이 문서는 이미 구현된 카카오톡 공유 기능을 이용해서 공유 퀘스트를 테스트하는 방법을 설명합니다.

## 📋 기존 공유 기능 확인

### ✅ 이미 구현된 기능들

1. **카카오톡 공유 기능** (`KakaoMapPage.dart`)
   - `_shareReviewViaKakao()` 함수로 리뷰 공유
   - `_notifyServerOfShare()` 함수로 서버에 공유 기록 전송
   - 앱 복귀 감지 로직 (`didChangeAppLifecycleState`)

2. **서버 API** (`api.py`)
   - `/shares/record` 엔드포인트로 공유 기록 처리
   - 공유 퀘스트 완료 처리 (`check_share_quest_completion`)

3. **퀘스트 시스템** (`quest_system.py`)
   - `SHARE_IMAGE` 퀘스트 타입
   - `create_share_image_quest()` 함수
   - `check_share_quest_completion()` 함수

## 🚀 테스트 방법

### 1. 백엔드 테스트 (API 레벨)

```bash
# tourist-data-analysis-main 디렉토리에서
python test_share_quest_simple.py
```

이 스크립트는 다음을 테스트합니다:
- 공유 기록 API 호출
- 퀘스트 상태 확인
- 보상 수령

### 2. 실제 Flutter 앱 테스트

#### 2-1. 앱 실행
```bash
cd flutter_test1
flutter run
```

#### 2-2. 테스트 시나리오

1. **로그인 및 게임 시작**
   - 앱 실행 후 로그인
   - 지도 페이지로 이동

2. **리뷰 작성**
   - 관광지 방문 (마커 클릭)
   - 사진 촬영 및 리뷰 작성
   - 리뷰 저장

3. **공유 기능 테스트**
   - 하단 FAB 메뉴에서 "나의 리뷰" 버튼 클릭
   - 작성한 리뷰 클릭하여 상세 보기
   - "카카오톡 공유하기" 버튼 클릭
   - 카카오톡 앱으로 이동하여 공유 완료
   - 앱으로 복귀

4. **퀘스트 완료 확인**
   - 하단 FAB 메뉴에서 "퀘스트" 버튼 클릭
   - "이미지 공유하기" 퀘스트가 완료 상태인지 확인
   - 보상 수령 버튼 클릭하여 +20점 받기

## 🔍 테스트 체크포인트

### 백엔드 체크포인트

1. **공유 기록 API 응답**
   ```bash
   curl -X POST http://localhost:8000/shares/record \
     -H "Content-Type: application/json" \
     -d '{
       "user_id": "test_user",
       "review_id": "test_review",
       "platform": "kakao"
     }'
   ```
   - 응답: `{"message": "공유가 성공적으로 기록되었습니다."}`

2. **퀘스트 상태 확인**
   ```bash
   curl http://localhost:8000/quests/test_user
   ```
   - 공유 퀘스트 상태가 `reward_ready`로 변경되었는지 확인

3. **보상 수령**
   ```bash
   curl -X POST http://localhost:8000/quests/{quest_id}/claim \
     -H "Content-Type: application/json" \
     -d '{"user_id": "test_user"}'
   ```
   - +20점이 정상적으로 추가되었는지 확인

### 프론트엔드 체크포인트

1. **공유 버튼 표시**
   - 리뷰 상세 보기에서 "카카오톡 공유하기" 버튼이 표시되는지 확인

2. **카카오톡 앱 전환**
   - 공유 버튼 클릭 시 카카오톡 앱으로 정상 전환되는지 확인

3. **앱 복귀 감지**
   - 카카오톡에서 공유 후 앱으로 복귀 시 서버에 알림이 전송되는지 확인

4. **퀘스트 완료 표시**
   - 퀘스트 창에서 "이미지 공유하기" 퀘스트가 완료 상태로 표시되는지 확인

5. **보상 수령**
   - 보상 수령 버튼 클릭 시 +20점이 정상적으로 추가되는지 확인

## 🐛 문제 해결

### 1. 공유 버튼이 표시되지 않는 경우

**원인**: 리뷰에 이미지가 없음
**해결**: 리뷰 작성 시 반드시 사진을 첨부하세요.

### 2. 카카오톡 앱 전환이 안 되는 경우

**원인**: 카카오톡이 설치되지 않음
**해결**: 카카오톡 앱을 설치하거나 웹 공유로 대체됩니다.

### 3. 퀘스트가 완료되지 않는 경우

**원인**: 서버에 공유 기록이 전송되지 않음
**해결**: 
- 앱 복귀 후 잠시 기다린 후 퀘스트 창을 다시 열어보세요
- 서버 로그에서 `/shares/record` API 호출을 확인하세요

### 4. 보상 수령이 안 되는 경우

**원인**: 퀘스트 상태가 `reward_ready`가 아님
**해결**: 
- 퀘스트 상태를 다시 확인하세요
- 서버에서 퀘스트 데이터를 직접 확인하세요

## 📊 예상 결과

### 성공적인 테스트 시나리오

1. ✅ 리뷰 작성 및 저장
2. ✅ 카카오톡 공유 실행
3. ✅ 앱 복귀 감지
4. ✅ 서버에 공유 기록 전송
5. ✅ 공유 퀘스트 상태 변경 (`active` → `reward_ready`)
6. ✅ 퀘스트 창에서 완료 상태 표시
7. ✅ +20점 보상 수령
8. ✅ 총 점수 업데이트

### 로그 확인 포인트

```bash
# 서버 로그에서 확인할 내용
🔗 공유 기록 완료: Review {review_id} by User {user_id}
   완료된 공유 퀘스트: 1개

# Flutter 앱 로그에서 확인할 내용
서버에 공유 완료를 알렸습니다: {review_id}
공유 흐름을 마치고 앱으로 복귀했습니다. 서버에 알림을 전송합니다.
```

## 🎯 테스트 완료 기준

- [ ] 카카오톡 공유 기능 정상 작동
- [ ] 앱 복귀 시 서버 알림 정상 전송
- [ ] 공유 퀘스트 상태 정상 변경
- [ ] +20점 보상 정상 수령
- [ ] 총 점수 정상 업데이트

---

**참고**: 이 테스트는 이미 구현된 공유 기능을 활용하므로, 별도의 코드 수정 없이 바로 테스트할 수 있습니다. 